{
  "name": "pocket-paste",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "paste": "bun core/src/cli.ts",
    "setup": "bun scripts/engine.ts",
    "probe": "bun scripts/probe.ts",
    "install-dev": "bun scripts/install-dev.ts",
    "studio": "bun scripts/studio.ts",
    "test": "bun test core/tests",
    "typecheck": "tsc --noEmit",
    "site:assets": "bun scripts/site-assets.ts",
    "site:build": "bun scripts/site.ts",
    "site:deploy": "bun scripts/site.ts && bunx wrangler pages deploy site --project-name peesuto --branch main"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "gifenc": "^1.0.3",
    "highlight.js": "11.12.0",
    "qrcode-generator": "1.4.4"
  }
}
