确定性渲染的意义不在于好看，而在于可复现。同一份声明、同一台机器、同一个引擎版本，出来的每一帧都应该逐字节一致；换一台机器也应该一致。做不到这一点，所有的视觉回归测试都是在比较两次随机结果，绿了不代表对，红了也不代表错。为了做到这一点，能动像素的每一个输入都要写进声明里：字体、字号、行高、换行位置、动画的每一个关键帧，连文本的度量也不能交给运行时去猜，而要在构建时用引擎自己的字形表量出来，再把量好的结果烤进源码。代价是构建变慢，好处是每一次失败都能定位到某一个输入的改变。这笔账我们算过：慢十秒的构建，换来的是再也不用盯着两张几乎一样的截图找区别。